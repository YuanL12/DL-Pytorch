## Data loading is often a bottelneck of DL, and using Multiple Processors can help accelarate it.

Syntax:

```Python
train_iter = data.DataLoader(mnist_train, batch_size, shuffle=True, num_workers=3)
```
使用多线程来读取更快